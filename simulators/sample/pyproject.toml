[project]
name = "sample"
version = "0.1.0"
description = "Gate physics simulator for the channel gate controller app"
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.9.1",
]

[project.scripts]
doover-app-run = "main:main"
